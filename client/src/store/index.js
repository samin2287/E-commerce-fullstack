/**
 * Store layer — structure only (no real state logic).
 * Wire Zustand/Redux/etc. here when backend is ready.
 */

export const storeConfig = {
  name: "verdant-client",
  version: 1,
};

export const slices = {
  // cart: undefined,
  // user: undefined,
};
